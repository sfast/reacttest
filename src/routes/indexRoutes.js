import Auth from '../layouts/Auth';
import Portal from '../layouts/Portal'

const indexRoutes =  [ { path: "/portal", component: Portal }, { path: "/auth", component: Auth }, ]

export default indexRoutes;
  